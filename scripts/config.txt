#!/usr/bin/env bash

JOB_NAME=gcp_000002

NUM_EPOCHS=10

CHECKPOINT_EPOCHS=${NUM_EPOCHS}
EVAL_FREQUENCY=${NUM_EPOCHS}

# GCP SETUP
GCS_BUCKET=gs://sales_forecast-187012

# LOCAL SETUP
TRAIN_FILE=data/train1.csv
EVAL_FILE=data/eval1.csv

JOB_DIR=output/${JOB_NAME}

GCS_JOB_DIR=${GCS_BUCKET}/${JOB_NAME}
GCS_TRAIN_FILE=${GCS_BUCKET}/${TRAIN_FILE}
GCS_EVAL_FILE=${GCS_BUCKET}/${EVAL_FILE}
